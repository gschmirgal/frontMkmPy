<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top d-print-none">
    <div class="container-fluid">
        
        <a class="d-flex align-items-center navbar-brand" href="{{ path('home')}}">
            <img src={{ asset("img/favicon.png")}} alt="Logo" class="me-2" id="logo-navbar">
            <span>MKM.py Front</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarScroll">
        <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll">
            <li class="nav-item">
                <a class="nav-link" aria-current="page" href="{{ path('home')}}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('expansion.list')}}">Editions</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('card.list')}}">Cartes</a>
            </li>
            
            <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Admin
            </a>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('admin.logs.mkmpy')}}">MKM.py logs</a></li>
                <li><a class="dropdown-item" href="{{ path('admin.logs.mkmoraclepy')}}">MKM OraclePy logs</a></li>
            </ul>
        </ul>
        <div class="mode-switch">
            <button title="Use dark mode" id="dark" class="btn btn-sm btn-default text-secondary">
                <i class="bi bi-moon"></i>
            </button>
            <button title="Use light mode" id="light" class="btn btn-sm btn-default text-secondary">
                <i class="bi bi-sun"></i>
            </button>
            <button title="Use system preferred mode" id="system" class="btn btn-sm btn-default text-secondary">
                <i class="bi bi-display"></i>
            </button>
        </div>
        <form class="d-flex" role="search" action="{{ path('search')}}">
            <input name="search" class="form-control me-2" type="search" placeholder="Search" aria-label="Search"/>
            <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
        </div>
    </div>
</nav>